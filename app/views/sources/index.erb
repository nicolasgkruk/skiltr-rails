<% provide(:title, 'All sources') %>
<h1>All sources</h1>

<%= will_paginate %>
<%= link_to "Add", sources_new_path , class: "btn btn-primary pull-right"%>
<ul class="items">
  <%= render @sources %>
</ul>
<%= will_paginate %>