 <li class="d-flex justify-content-between align-items-center">
  <div>
    <h4><%= link_to project.title, project_path(project.id), title: "View project's signs" %></h4>
    <span><%= project.description%></span>
  </div>
  <div>
    <%= link_to "edit", edit_project_path(project.id) %> -
    <%= link_to "delete", project, method: :delete, data: { confirm: "Please take into account that by deleting this project you will also be deleting as well all signs you might have created which are associated to it. Do you confirm?" } %>
  </div>
</li>